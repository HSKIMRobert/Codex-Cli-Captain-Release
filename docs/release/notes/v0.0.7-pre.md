# v0.0.7-pre

> Pre-release note: `v0.0.7-pre` keeps the public docs aligned while the next presentation slice lands. The emphasis is on making the README positioning copy easier to notice, keeping the 0.0.7 plan smoke-backed, and preserving a clear operator-facing paper trail.

## Theme

`0.0.7-pre` is a documentation-presentation and operator-clarity slice with a runtime repair for LongWay checklist visibility and specialist routing.

The immediate goal is to keep the README positioning copy at the top of the source and release README files, but make that copy stand out more clearly in English, Korean, and Japanese with simple Markdown formatting. The release record keeps the smoke evidence as history, then separates the runtime slices that have already landed from the remaining 0.0.7 documentation and presentation work.

## Historical Smoke Baseline

Companion operator smoke already covers the core route and operator-text surfaces:

- `check-install` passed.
- `way` routed to `ccc_tactician`.
- `explore` routed to `ccc_scout`.
- `git commit` routed to `ccc_companion_operator`.
- code mutation routed to `ccc_raider`.
- docs/operator-text previously routed to `ccc_raider` instead of `ccc_scribe`.
- `ccc start` previously did not show the selected route or agent until `ccc status`.
- `ccc status` previously showed `Current Item` but not a checklist breakdown.

## Landed Runtime Fixes

- docs/operator-text routing now prefers `ccc_scribe`.
- `ccc start` now shows the selected route and agent at launch time.
- `ccc status` now shows a checklist-style completed/current/remaining breakdown.
- `ccc checklist --text` now provides a separate checklist-only surface: a `LongWay` header plus separate `[x]` / `[>]` / `[ ]` item rows for a run.
- terminal `ccc subagent-update --text` now persists the active LongWay phase completion before rendering status, so the completion output prints the checked checklist item.
- multi-item LongWay completion now advances the active item in order instead of completing the last pending item, and checklist labels prefer persisted task titles before falling back to internal phase names.
- the packaged `$cap` skill now requires `ccc checklist --text` to be shown as a separate operator-visible lifecycle block immediately after run start, after each terminal fan-in, after the following `merged` update, and before the next task/replan/orchestration step.
- the public release repository now keeps only the user-facing install/download surface; release build and verification scripts moved to the source repo under `scripts/release/`, and packaged assets omit internal `scripts/` plus install/release docs.
- code/config repair requests with incidental git or release context stay on `ccc_raider`; explicit operator-side git/gh release mutations still route to `ccc_companion_operator`.
- stale generated defaults now upgrade the `code specialist`/raider role from `gpt-5.5` `medium` to `gpt-5.5` `high`, matching the recommended role table.

## Ledger

- [x] README positioning copy is now visually emphasized at the top of the source and release README files in English, Korean, and Japanese.
- [x] The 0.0.7 work plan records the smoke baseline and the landed runtime fixes separately from remaining documentation work.
- [x] The routing/start/status runtime fixes are documented as landed, not pending.
- [x] LongWay checklist output is visible at run start and checked after terminal subagent completion.
- [x] `$cap` lifecycle guidance now prints the standalone checklist block instead of relying on folded command transcript output.
- [x] Multi-item checklist smoke verifies ordered `[x]` / `[>]` / `[ ]` transitions and task-title labels.
- [x] Public release repo surface is minimized; internal release tooling and docs are source-owned, while assets still include the required packaged `$cap` skill.
- [x] Routing smoke verifies code repair goes to `ccc_raider` and explicit GitHub release mutation goes to `ccc_companion_operator`.
- [ ] The remaining 0.0.7 work stays focused on the scribe handoff brief, commit-message style, progress refinements, and cleanup/modularity review.

## Validation

- `cargo test -p ccc` passed: 159 tests.
- Local installed `0.0.7-pre` smoke passed: `ccc checklist --text` printed only `LongWay` plus `[>] mutate`.
- Local installed `0.0.7-pre` smoke passed: after terminal `ccc subagent-update --quiet`, `ccc checklist --text` printed only `LongWay` plus `[x] mutate`.
- Local release-binary multi-item smoke passed: checklist rendered ordered task-title rows and advanced from `[>]` to `[x]` without consuming later items.
- Local installed `0.0.7-pre` routing smoke passed: explicit `gh release create` routed to `ccc_companion_operator`.
- `git diff --check` passed.

## Asset Repair - 2026-04-30

- Rebuilt and re-uploaded the `v0.0.7-pre` release tarballs after confirming the published assets still contained `0.0.6-pre` binaries on Linux, macOS, and Windows.
- `install.sh` platform detection did not require a release-repo change; the release packaging script now selects the matching target binary for each asset.
- Re-downloaded the uploaded assets and verified: `darwin-arm64` is Mach-O arm64, `darwin-x86_64` is Mach-O x86_64, `linux-arm64` is ELF aarch64, `linux-x86_64` is ELF x86-64, and `windows-x86_64` is PE32+ x86-64; all five binaries report `0.0.7-pre`.
- Uploaded asset digests: `ccc-0.0.7-pre-darwin-arm64.tar.gz` `sha256:c1e0d208128910ed64e9df1652a8a85a195e61b20abe99685b100fb23b208d4e`; `ccc-0.0.7-pre-darwin-x86_64.tar.gz` `sha256:c3337f0920e9b32d376b7d8e72a79f8e2a8339939a2515c359ceed6cc91c07d0`; `ccc-0.0.7-pre-linux-arm64.tar.gz` `sha256:22d3331a900d548165aa891d5192fcbe649e1179a99c0649a5ee384f83ba88e7`; `ccc-0.0.7-pre-linux-x86_64.tar.gz` `sha256:6dd30e02f5862dec3ee6a5aad8672567bb8120b961feaee1a34fb510c3957990`; `ccc-0.0.7-pre-windows-x86_64.tar.gz` `sha256:ac5a33335aeb82bc0d0ad81086221be98d4507dc0046a4bcbef8450cbbd8a6e4`.
